<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="App1.DeleteClientModalPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ContentPage.Content>
        <StackLayout>
            <ListView
                x:Name="ClientsList"
                HeightRequest="100"
                ItemSelected="ClientsList_ItemSelected"
                ItemsSource="{Binding Clients}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout Orientation="Horizontal">
                                    <Label Text="{Binding Surname}" />
                                    <Label Text="{Binding Initials}" />
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button Clicked="Button_Clicked" Text="Delete Client" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>